json.pay_item do
	json.id 											@pay_item.try(:id)
	json.company_id 							@pay_item.try(:company_id)
	json.eligible_from 						@pay_item.try(:eligible_from)
	json.name 										@pay_item.try(:name)
	json.code 										@pay_item.try(:code)
	json.item_type 								@pay_item.try(:item_type)
	json.calculation_type 				@pay_item.try(:calculation_type)
	json.is_active 								@pay_item.try(:is_active)
	json.show_in_slip 						@pay_item.try(:show_in_slip)
	json.part_of_other 						@pay_item.try(:part_of_other)
	json.is_taxable 							@pay_item.try(:is_taxable)
	json.exempted_tax_percentage 	@pay_item.try(:exempted_tax_percentage)
	json.formula 									@pay_item.try(:formula)
	json.is_bonus 								@pay_item.try(:is_bonus)
	json.bonus_date 							@pay_item.try(:bonus_date)
	json.bonus_month 							@pay_item.try(:bonus_month)
	json.bonus_type 							@pay_item.try(:bonus_type)
	json.formula_with_code 				@pay_item.try(:formula_with_code)
	json.description 							@pay_item.try(:description)
	json.sort_order 							@pay_item.try(:sort_order)
	json.part_of_gross_salary 		@pay_item.try(:part_of_gross_salary)
	json.prediction_tax_impact 		@pay_item.try(:prediction_tax_impact)
	json.name_in_urdu 						@pay_item.try(:name_in_urdu)
	json.is_urdu 									@pay_item.try(:is_urdu)
	json.annualize 								@pay_item.try(:annualize)
end